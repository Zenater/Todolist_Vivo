import {mul} from "./native7";

test('mul',()=> {
    const a=10;
    const b=2;
    const result=mul(a,b)
    expect(result).toBe(20)
})test('saloreReducer',()=> {
   const sumAction =sumAC(payload:330}
    expect(saloreReducer(570,sumAction)).toBe(900)
}
